
# Radix LLM Demo — scrypto assistant

#LINK-https://vsdc1fj9-8502.inc1.devtunnels.ms/

## Mission
Build the smallest possible proof that an LLM can be coached to output compile-clean Scrypto (.rs) blueprints and run tests automatically.

## Data Foundations
- **Raw data folder:** `kb/raw`  
- **Cleaned data folder:** `kb/cleaned`
so so- **File counts:**
	- Raw: <!-- TODO: update with actual count -->
	- Cleaned: <!-- TODO: update with actual count -->
- **Timestamps:**
	- Last updated: <!-- TODO: update with timestamp -->

## How to run (single command)
From repo root:
```bash
python scripts/prompt_to_code.py --prompt-file scripts/prompt.txt
```

## Output
- Generated code and test artifacts are saved in the `output/` folder.
- Results and retry info are saved in `results.json`.

## Test Log (Section 2)
Paste the green test log output here after running the script:
```
<test log here>
```
